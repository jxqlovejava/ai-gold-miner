# =============================================================================
# AI Gold Miner 环境配置
# =============================================================================
# 使用方式：
#   1. 最快体验：cp .env.demo .env && gold-miner --demo scan
#   2. 完整功能：复制本文件为 .env，按 tiers 填入 API keys
# =============================================================================

# -----------------------------------------------------------------------------
# [demo] 零配置体验（必填基础项）
# -----------------------------------------------------------------------------
# Demo 模式会自动跳过需要 API key 的功能，仅运行技术/基本面信号。
INITIAL_CAPITAL_USD=100000
MAX_POSITION_PCT=0.8
STOP_LOSS_PCT=0.03
TAKE_PROFIT_PCT=0.06
RISK_PROFILE=moderate

# -----------------------------------------------------------------------------
# [api_keys] 进阶数据（可选，留空则关闭对应功能）
# -----------------------------------------------------------------------------
# 至少配置一个以解锁对应维度。留空不会崩溃，只会跳过该数据源。
FRED_API_KEY=
NEWS_API_KEY=
TAVILY_API_KEY=
LLM_API_KEY=

# LLM 服务地址（默认 DeepSeek）
LLM_API_BASE=https://api.deepseek.com/anthropic
LLM_MODEL=deepseek-v4-pro

# -----------------------------------------------------------------------------
# [data_sources] 数据源符号（通常无需修改）
# -----------------------------------------------------------------------------
YAHOO_SYMBOL_SPOT=XAUUSD=X
YAHOO_SYMBOL_GLD=GLD
YAHOO_SYMBOL_IAU=IAU
YAHOO_SYMBOL_DXY=DX-Y.NYB

# -----------------------------------------------------------------------------
# [notification] 通知（可选）
# -----------------------------------------------------------------------------
WECHAT_WEBHOOK_URL=
ENABLE_NOTIFICATION=false

# -----------------------------------------------------------------------------
# [features] 功能开关（可选）
# -----------------------------------------------------------------------------
ENABLE_AUTO_TRACKING=true
POLYMARKET_ENABLED=true
POLYMARKET_MIN_VOLUME=500
POLYMARKET_MAX_MARKETS=20

# -----------------------------------------------------------------------------
# [proxy] 代理（可选，仅在需要访问外网数据源时配置）
# -----------------------------------------------------------------------------
MIHOMO_SUB_URL=

# -----------------------------------------------------------------------------
# [agent] 定时任务代理（可选）
# -----------------------------------------------------------------------------
AGENT_ENABLED=false
AGENT_TIMEZONE=Asia/Shanghai
AGENT_SCHEDULE_PRE_MARKET=08:00
AGENT_SCHEDULE_POST_OPEN=09:30
AGENT_SCHEDULE_CLOSING=14:30
AGENT_SCHEDULE_EVENT_SCAN=20:30
AGENT_SCHEDULE_WEEKLY=sun-21:00
