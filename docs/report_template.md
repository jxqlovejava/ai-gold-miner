# 金价分析报告固定模板

> 2026-08-11 建立（用户需求 3）：**每次金价分析输出格式完全一致**，任何板块不得整体省略。
> 对齐 `skills/gold-analysis-pipeline/SKILL.md` 的分析输出模板（九步管线），本文件补充具体的标题、表格列、emoji 与空态规则。

---

## 空态规则（核心）

- **每个板块必须输出标题**，无数据时写 `（本期无触发）` / `（本期无违规）` 等占位，**禁止整块省略**。
- 方向 emoji 固定：🟢 看多 / 🔴 看空 / ⚪ 中性
- 信号强弱：🟡（弱）/ 一般不加（中）/ 无需（强，用文字标）
- 决策：📌 | Agent：🐮/🐻/💼 | 违规：⚠️

---

## 板块固定顺序与格式

### 标题
```
# 🥇 金价完整分析 · YYYY-MM-DD HH:MM
```

### 行情概览引文（固定一行）
```
> 积存金 **¥958.81** | 国际 $4408/oz
```

### 1. 决策摘要
```
## 📌 决策: **持有** | 综合评分 +0.30 | 置信度 75%
建议仓位: 60%
止损: 921.63
止盈: —
```
方向词固定映射：`buy→买入 / sell→卖出 / hold→持有 / neutral→观望`。

### 1b. 金价目标区间预测（综合分析结论必含，2026-08-11 起）
**每次分析必须预测未来涨跌幅度**——分情景给出积存金目标区间（元/g），格式：
```
## 🎯 金价目标区间预测（未来 1-3 个月）
当前价 ¥958.81 | 对应 XAUUSD $4,408/oz

| 情景 | 概率 | 触发条件 | 积存金目标区间(元/g) | 对应 XAUUSD |
|------|------|---------|---------------------|------------|
| 看多突破 | 40% | CPI温和+央行购金延续+停火未续 | **1000-1200** | $4,600-5,500 |
| 高位震荡 | 40% | CPI符合预期 | 950-1000 | $4,300-4,600 |
| 回调修正 | 20% | CPI过热+聪明钱流出 | 900-950 | $4,000-4,300 |
```
规则：
- **区间必须基于当前价推导**（突破关口的下一档心理位 / 支撑位 / 历史波动幅度），不凭感觉。
- 每情景给出**概率**（合计=100%）与**触发条件**（事件/资金/技术确认）。
- 参考档位：心理整数关口（950/1000/1100/1200 元/g）、20日高点、ATR 移动止盈位、成本净保本价。
- 预测与交易建议联动：看多区间冲关 → 条件单上移；回调区间 → 低吸档承接。

### 2. 维度信号
```
## 维度信号
维度 | 方向 | 看多/看空/中性 | 均分 | 说明
```
用 `b.format_dimension_table()` 输出。**引用程序化表格汇总行，禁止手动数信号方向**（见 CLAUDE.md 信号维度计数规则）。

**技术面必含「🀄 缠论结构」子板块**（2026-08-12 起强制，见 SKILL.md 2.2）：
```
🀄 缠论结构（600天窗口 / 397根日线）：
- 结构：X 笔 + Y 中枢（ZG=… / ZD=… / ZZ=…），状态=形成/未形成
- 买卖点：缠论X买/卖（方向, score）— 日期@价位, 置信度 N%
- 当前位置：价格处于中枢上/下/内, 结构偏多/空
- 映射：X买 → 分批建仓锚点参考 / X卖 → 减仓止盈参考
```
- 无中枢/无买卖点时写 **「（本期无触发）」**，板块标题不省略。
- 数据源必须 `SpotGoldFetcher().fetch(days=600)`；积存金短窗口（<100根日线）无法形成结构，禁止用作缠论输入。

### 3. Agent 博弈
```
## Agent 博弈
🐮 **BullAgent** (信心 70%)
  ✓ 论据1
  ✓ 论据2
  ✓ 论据3
🐻 **BearAgent** (信心 65%)
  ✗ 论据1
  ✗ 论据2
  ✗ 论据3
```
无发言写 `（本期无多头发言）`。

### 4. 军规自查
```
## 军规自查
通过 20/32
⚠️ r011 警惕一边倒：信号方向分布...
（本期无违规）
```

### 5. Munger 模型
```
## Munger 模型
- **市场先生**: 理由...
- **安全边际**: 理由...
```

### 6. 画像匹配
```
## 画像匹配
✅ 兼容
- 约束说明...
```
`✅ 兼容` / `⚠️ 超出约束`。

### 7. 条件单审查
```
## 条件单审查
- co_xxx 保留 原因...
```

### 8. 后续关注
```
## 后续关注
- 美国CPI 08-12 20:30
```

### 9. 经验提醒
```
## 📚 经验提醒
- 提醒...
```

---

## 附：HTML 报告

分析完成后在终端末尾输出本地 HTML 报告地址（复制即可浏览器打开）：

```bash
python3 scripts/render_report_html.py
```

生成 `data/output/金价分析_YYYY-MM-DD.html` 并在终端打印 `file://{绝对路径}`。
