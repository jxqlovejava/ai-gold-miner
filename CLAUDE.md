# AI Gold Miner — 项目上下文与决策约束

## 投资者画像

> 私密画像数据分两份文件维护，避免数字重复导致不一致：
> - **定性画像**（风险偏好、交易风格、信源偏好、笔记）存于 `data/private/investor_profile.md`。
> - **定量持仓**（持仓量、成本均价、止损价、额度）存于 `data/private/portfolio.yaml`，作为唯一数字真相源。
>
> 每次涉及金价分析、交易建议、持仓决策时，先读取 `data/private/investor_profile.md`，再读取 `data/private/portfolio.yaml` 获取当前持仓数字。
> 若文件不存在，分别使用 `data/investor_profile.example.md` 和 `data/portfolio.example.yaml` 作为占位，并提示用户填充真实数据。

### 公开约束（不依赖私密文件）

| 约束项 | 公开规则 |
|--------|---------|
| 单品种仓位上限 | 20 万元 |
| 总敞口上限 | 所有黄金头寸合计不超过 80%，保留 20% 现金 |
| 杠杆 | 不借钱，仅使用自有资金 |
| 方向 | 仅做多 |
| 硬止损 | 成本价的 -30%（具体价位见投资者画像） |
| 风险偏好 | 平衡型（最大回撤容忍 10-20%） |
| 持仓周期 | 1-3 年 |
| 信源要求 | 多源交叉验证，重点识别机构「带节奏」 |

---

## 工具使用规则

- **默认搜索工具**：本项目所有网络搜索默认使用 `anysearch` skill，除非用户明确指定其他工具或该工具不可用。
- 当 `anysearch` 不可用时，依次 fallback 到 `web-access` skill 或 `WebSearch`。
- **自动提交**：完整实现一个功能或 fix 解决问题后，自动调用 `/gcp` 命令提交并推送，无需等待用户确认，直接执行。
- **自主修Bug**：运行过程中发现系统 Bug 或错误（如导入报错、脚本异常、数据格式不匹配等），应尝试自行修复，而非仅报告错误等待用户指示。修复后继续原任务流程。

### 日期查询硬规则

**涉及日期/时间的查询，搜之前必须走三步校验：**

1. **以系统 `currentDate` 为唯一真相源** — 提取年/月/日/星期，搜索关键词中不硬编码年份
2. **搜后反查星期** — 得到日期后立即算当日周几，确认不是周末/节假日（官方数据不会在非工作日发布）
3. **关键事件日期（PCE、非农、CPI、FOMC 等）必须查官网 release schedule 确认**，不以搜索摘要为最终依据
4. **🆕 DOW 参考表强制校验** — 每次分析输出前，必须先运行 `PYTHONPATH=src python scripts/validate_calendar_dates.py --ref-table 30`，将输出的事件 DOW 参考表与报告中提到的所有事件日期逐条对照。报告中任何"周X + 事件名"的组合必须与参考表中的 ET 星期完全一致。**此规则不可绕过——代码层面 `add_event` 已对已知确定性事件（如初请失业金=周四、FOMC决议=周三、非农=周五）做 DOW 硬阻断**。

> 背景：曾因忽略系统年份（2026）而用 2025 年关键词搜索，叠加未校验星期，导致 PCE 日期输出错误（周五 vs 实际周四）。
> 🆕 2026-07-18：分析报告中"周三初请失业金"实际为周四（7/23），根因：输出时未用 DOW 参考表逐条校验。已通过 `calendar_time_rules.check_event_dow()` + `--ref-table` 工具链修复。

### 快速演变事件搜索规则

**geo/policy_shift/trade_war/fed_emergency 等持续演变事件**，不能按"搜索一次→写入 actual→永久有效"处理。标准三步校验（DOW/官网/交叉确认）不足以应对状态变化。

**铁律**：

1. **时间约束搜索** — 搜索必须限定时间范围，不搜不带日期约束的查询：
   - 正确: `"美伊谈判" after:2026-07-14 site:reuters.com`
   - 错误: `美伊谈判最新进展`（按 SEO 排序，"大新闻"压制逆转报道）
2. **逆转/修正优先搜索** — 主动搜索事件被逆转/撤销/修正的信号：
   - `"Hormuz fee" reversal OR backtrack OR withdraw OR cancel OR 撤销`
   - `"Iran policy" update OR latest OR "as of"`
3. **多时点交叉验证** — 对比三个时间点的报道：
   - 初始公告报道 → 中间状态 → 最新报道
   - 若三者在 72h 内不一致，以最新 T0/T1 来源为准
4. **搜索排序意识** — 搜索引擎偏向"大新闻"（初始公告），压制"小更新"（逆转）。
   不要仅依赖搜索结果第一页或摘要位置判断重要性。**主动向下翻页**或使用不同查询找更新。
5. **每次第〇步都重新验证** — 对于 event_type 在 `FAST_EVOLVING_TYPES` 中且 `actual_updated_at` 超过阈值的
   事件，调用 `EarlyWarningEngine().check_stale_events(lookback_days=7)` 自动发现，
   然后对每个 stale 事件用上述规则重新搜索验证。

> 背景：2026-07-16 分析中美伊冲突使用 7/13 的过时信息（Trump 征收 20% Hormuz 费），
> 未发现 7/14 已撤销收费改为海湾投资协议。根因：搜索引擎返回初始大新闻压制逆转报道 +
> 无 staleness 检测机制。

## 输出规则

每次输出交易建议或决策分析时，必须包含以下三个板块，并**使用中文方向术语（看多/看空/观望），不直接使用 long/short/neutral**：

### 1. 军规自查
引用具体规则 ID（r001-r030），标注每条规则对该决策的判定（✅/⚠️/❌）。完整军规见 [`docs/doctrine.md`](docs/doctrine.md)。

### 2. Munger 模型
引用 2-3 个与当前决策最相关的思维模型，说明如何应用。

### 3. 画像匹配
明确说明建议是否在该用户的约束范围内（仓位上限 / 持仓周期 / 风险偏好 / 硬止损规则）。

---

## 输出格式规范

**🔴 每次金价分析必须输出完整报告，不可省略任何板块给摘要。**

完整报告包含：多维度信号采集（8维逐项说明）→ Agent博弈（Bull/Bear/PM三方）→ 军规自查（r001-r030）→ Munger模型（2-3个）→ 画像匹配 → 事实vs解释 → 后续事件关注 → 最终建议。

每次运行项目 pipeline（如 `gold-miner scan`）后，输出不能只给结论。详细格式规范（Agent 博弈披露格式、8 维信号逐项说明、API 失败披露）见 [AGENTS.md](AGENTS.md)。

---

## 强制分析流程

每次涉及金价分析、交易建议、持仓决策时，必须走完项目内置的完整 pipeline，禁止基于单一维度或未经交叉验证的信息直接下结论。

### 第〇步：事件日历同步（强制，每次分析前必须先执行）

**在开始任何市场分析之前**，必须先将事件日历与最新实际结果同步。

**0.0 日历日期+钟点校验（每次第〇步的第一步）**

在同步事件结果之前，先运行校验脚本：

```bash
PYTHONPATH=src python scripts/validate_calendar_dates.py
```

- 若报 🔴 错误（周末 / **钟点双重换算** / 宏观数据非惯例钟点）：**必须修复后再继续**，不得跳过
- 若报 🟡 警告：人工确认，必要时查官方发布日历
- 校验通过后继续 0.1

**背景**：
- 2026-07-09：6 个日期 DOW 错误（CPI/初请/密歇根/PCE/PPI 等）。根因：写入未校验 DOW+未查官网。
- **2026-07-15：沃什众议院听证钟点错误**。官方 10:00 ET = 北京 22:00 (7/14)；日历误存 22:00 ET → 展示成北京 7/15 10:00，导致误判「会还没开」。根因：**双重换算**（先换成北京 22:00，再把 22:00 当 ET 写回）。

**日历写入铁律（日期 + 钟点，缺一不可）**：

1. **存储只认美东墙上钟点** `scheduled_at`（如 `2026-07-14T10:00:00-04:00`），禁止把北京小时数直接写入。
2. **写入前必须打印双列** `ET | 北京`（`dual_clock_str` / 事件 `.dual_clock_str`），两列都合理才落盘。
3. **三步校验**：DOW（美东星期）→ 官网/notice 原文钟点与时区 → 交叉确认（禁止只信搜索摘要）。
4. **国会/听证**：美东几乎总是上午（常见 10:00 ET）；若写成 ET 18:00+ 会被 `validate_calendar_dates` **直接报错**（典型双重换算形态）。
5. **BLS 数据（CPI/PPI/非农等）**：惯例 08:30 ET；写成晚间 ET 报错。
6. **代码写入**：优先 `make_et_iso(y,m,d,h,mi)`；`EventCalendar.add_event` 默认拒绝硬错误（`force=True` 仅历史回填）。

| 错误写法 | 正确写法 |
|----------|----------|
| 听说「北京晚上10点开」→ 存 `22:00-04:00` | 官网 10:00 ET → 存 `10:00-04:00` → 北京自动成 22:00 |
| 只展示北京时间做决策 | 决策引用必须 **ET + 北京** 同时出现 |

> 注意：地缘冲突、政策突变等无预排日期的事件由第一步的 `NewsSignalGenerator`（消息面）实时捕获。这些事件会被捕获为 `Signal`，但不会自动写入日历。重大事件的回写规则如下：

**重大事件判定标准**（满足任一即为重大）：
- 对金价日内波动 > 2%
- 涉及主要产油国/霍尔木兹海峡/大国军事冲突
- 央行紧急政策声明/关税突变/资本管制
- 消息面 `SignalStrength == STRONG` 且 `_is_geopolitical() == True`

**重大事件回写 + 演变追踪**：
1. 追加到 `calendar_events.jsonl`（`event_type: "geo"`，`actual` 记录事件经过）
2. 检查是否需要无效化相关日历事件
3. **创建对应的 monitor 事件**跟踪后续演变（因为地缘事件是持续过程，不是单点）：
   - `name`: `"观测: [事件名]后续演变"`
   - `trigger_condition`: 描述需要跟踪的演变方向（升级/缓和/外溢）
   - `check_frequency`: `"on_analysis"`
   - `action_on_trigger`: 演变对金价的影响评估框架
   - `expires_at`: 建议 30 天后，届时重新评估

示例：美伊停火破裂
```
日历追加: 7/7 美伊停火破裂 (actual=特朗普宣布over+伊朗攻击+油价飙5%)
无效化:   8/18 停火到期 → actual=已失效
Monitor:  观测: 美伊局势后续演变
          trigger="霍尔木兹海峡航运受阻/油价持续>80$/美军增派/伊朗封锁海峡"
          action="地缘升级→避险利多但油价推高加息概率→净影响需结合当时宏观判断"
```

1. 调用 `EarlyWarningEngine().check_recent_results(lookback_days=7)` 查询最近 7 天内已发布但 `actual` 为空的事件
2. 对每个待查事件，按优先级搜索权威来源获取实际结果：
   - **T0 优先**：BLS.gov、FRED、CME FedWatch、FederalReserve.gov、BEA.gov
   - **T2 备用**：Reuters、Bloomberg、Kitco 报道
   - 禁止仅依赖搜索引擎摘要
3. 将实际结果写入 `calendar_events.jsonl`（调用 `calendar.update_event_result()`）
4. 调用 `EarlyWarningEngine().get_active_monitors()` 查询所有活跃的 monitor 事件
5. 逐一评估每个 active monitor 的 `trigger_condition` 是否已满足（对照当前市场数据）
6. 对已触发的 monitor：调用 `calendar.close_monitor()` → 记录结果到日历
6.5 **检查 fast-evolving 事件的 staleness（新增）**:
    调用 `EarlyWarningEngine().check_stale_events(lookback_days=7)` 查询需要重新验证的事件。
    对每个返回的事件：
      a. 确认事件类型是否为持续演变的（geo/policy_shift/trade_war/fed_emergency）
      b. 使用**时间约束搜索**获取最新状态（参见「快速演变事件搜索规则」）
      c. 主动搜索逆转/修正/撤销报道（reversal/backtrack/withdraw/cancel）
      d. 必须从 ≥2 个独立来源交叉确认
      e. 如果最新状态与 `actual` 不同：
         - 调用 `calendar.update_event_result()` 更新（自动保存历史到 `actual_history`）
         - 在同步表中标注 `📝已更新 (旧值→新值)`
      f. 如果最新状态与 `actual` 一致：无需操作（`actual_updated_at` 已在之前更新）
7. 输出三张表：

**事件日历同步表**（含修订记录）:

| 事件名称 | 发布日期 | 实际结果 | 修订记录 | 对金价影响 | 来源 |
|----------|----------|----------|----------|-----------|------|
| FOMC会议纪要 | 7/9凌晨 | 9/18官员支持加息，鹰派 | — | 利空 | [T0] Federal Reserve |
| ISM服务业PMI | 7/6 | 54.0 vs 预期54.2 | — | 略利空 | [T0] ISM |

**Staleness 重新验证表**（新增，仅针对 fast-evolving 事件）:

| 事件名称 | 原记录结果 | 最新状态 | 变更 | 最新来源 |
|----------|-----------|---------|------|---------|
| 美伊冲突: Trump Hormuz 费 | 7/13 征收 20% Hormuz 费+封锁 | 7/14 撤销 20% 费, 改为海湾投资协议 | 📝 逆转（费用取消） | [T1] Reuters/Bloomberg |

> 若 `check_stale_events()` 返回空列表或所有事件已验证无变化，此表可标注「✅ 所有 fast-evolving 事件均为最新状态」。

**Monitor 事件检查表**:

| Monitor 名称 | 触发条件 | 当前状态 | 是否触发 | 动作 |
|-------------|----------|----------|----------|------|
| 观测: ETF资金流回正 | 国际黄金ETF转为净流入 | 仍净流出 | ❌ 未触发 | 保持 active |

8. 同步完成后，将已发生事件结果 + monitor 触发结果 + staleness 重新验证结果按时效性衰减权重注入第一步信号采集
    （注：fast-evolving 事件若 `needs_reverify=True` 且未完成重新验证，其信号权重会自动被 `staleness_penalty` 减半）

#### 时效性衰减权重规则

**越新的事件对短期金价影响越大**。事件结果注入第一步时，必须按时间衰减加权：

| 事件距今 | 权重 | 信号强度 | 说明 |
|----------|------|---------|------|
| <24h | **1.0** | STRONG | 最新，市场正在定价中 |
| 24-48h | **0.7** | MODERATE | 已大部分消化 |
| 48-72h | **0.5** | MODERATE | 影响递减中 |
| 3-7d | **0.3** | WEAK | 已基本定价，仅作背景参考 |
| >7d | 0 | — | 过时，不纳入 |

9. 输出「事件结果时效性加权表」：

| 事件 | 发布时间 | 距今 | 权重 | 实际 vs 预期 | 金价方向 | 加权得分 |
|------|----------|------|------|-------------|---------|---------|
| FOMC纪要 | 7/9 02:00 | 10h | 1.0 | 鹰派（9/18支持加息） | 看空 | -1.0 |
| ISM服务业 | 7/6 20:00 | 3d | 0.3 | 54.0 vs 预期54.2 (略低) | 略看空 | -0.3 |
| 非农 | 7/2 20:30 | 7d | 0.3 | +5.7万 vs 预期+11.3万 (大幅miss) | 看多 | +0.3 |

> 加权综合信号 = Σ(方向得分 × 权重) / Σ权重。方向得分：看多=+1，看空=-1，中性=0。
> 上例：(−1.0×1.0 + −0.3×0.3 + +0.3×0.3) / (1.0+0.3+0.3) = −1.0/1.6 = −0.625 → **偏空**

> ⚠️ **铁律**：
> - 不执行事件日历同步 = 基于过时信息做决策
> - 不执行 monitor 检查 = 丢失上次分析设定的跟进条件
> - 消息面捕获的**重大**地缘/政策事件 → 回写日历（持久化） + 无效化相关事件 + 创建 monitor 跟踪演变
> - 如果用户上次分析后间隔了数天，中间可能有多起事件发生。**最新事件（<24h）的权重必须是 7 天前事件的 3.3 倍。**

### 第〇·五步：深度新闻搜索（强制，anysearch + last30days-cn）

在第〇步事件日历同步完成后、第一步信号采集之前，必须执行深度新闻搜索。

**执行方式**：
1. 运行 `PYTHONPATH=src python -m src.gold_miner.sentinel --mode deep-news-queries` 获取搜索计划
2. 按输出中 P0 → P1 → P2 的优先级顺序，对每个主题：
   - **anysearch**：执行 `anysearch` skill 搜索国际源（每条 query 取前 5 结果）
   - **last30days-cn**：执行 `last30days-cn` skill 搜索中文源（每条 keyword 取主要报道）
3. 搜索结果汇总为「深度新闻扫描」表：

| 主题 | 优先级 | 关键发现 | 金价影响方向 | 来源 |
|------|--------|---------|------------|------|
| 地缘冲突 | P0 | 美伊冲突升级/缓和/... | 看多/看空/中性 | anysearch+last30days |
| 美联储政策 | P0 | ... | ... | ... |

4. 只有在**两个独立来源**（anysearch + last30days 各算一个维度）均无异常发现时，才可标注「无重大突发新闻」
5. 此步骤发现的突发新闻直接注入第一步的「消息面」维度，优先级高于定时 cron 推送

> 🆕 2026-07-18：Hermes 端 news_monitor.py 负责实时关键词告警（每15分钟），本地 deep_news.py 负责分析前深度扫描。两者互补：前者保证不错过突发，后者保证不遗漏细节。


### 第一步：多维度信号采集（强制）

| 维度 | 工具 | 输出内容 |
|------|------|---------|
| **近期事件（时效性加权）** | `RecentEventSignalGenerator` | 第〇步同步的事件结果，按时间衰减加权 |
| 技术面 | `TechnicalAnalyzer` | RSI、MACD、布林带、20 日区间、ATR |
| 基本面 | `FundamentalAnalyzer` | DXY、实际利率、通胀预期、金银比、央行购金 |
| 消息面 | `NewsSignalGenerator` | 24h 新闻信号 + 情感打分 |
| 情绪面 | `SentimentAnalyzer` | 期货持仓、CFTC COT |
| ETF 资金流 | `EtfFlowSignalGenerator` | 黄金 ETF 净流入/流出 |

> 第〇步同步的事件结果已按时效性衰减注入 `recent_events` 维度（权重最高）。基本面/消息面中涉及相同事件时，应引用第〇步表中的加权结论而非独立评估。

### 第二步：Agent 博弈（强制）

调用并输出：

- `BullAgent.analyze(bundle)` — 看多理由
- `BearAgent.analyze(bundle)` — 看空理由
- `PortfolioManager` — 仓位建议

具体披露格式见 [AGENTS.md](AGENTS.md)。

### 第三步：Source Truth Verification（强制）

所有外部信息必须标注来源等级。层级定义与验证动作见下文「信息验证协议」。

### 第四步：军规审查（强制）

用 `DoctrineChecker` 跑 r001-r030，输出每条规则判定 ✅/⚠️/❌。完整军规见 [`docs/doctrine.md`](docs/doctrine.md)。

### 第五步：Munger 模型（强制）

每次输出必须引用 2-3 个最相关的 Munger 思维模型，说明如何应用。

### 第六步：画像匹配（强制）

每次建议必须明确说明是否在用户约束范围内：

- 仓位上限 20 万
- 持仓周期 1-3 年
- 风险偏好 平衡型（回撤 10-20%）
- 硬止损规则：成本价 -30%（具体价位读取自投资者画像）
- 单一黄金品种集中度

### 第七步：区分事实与解释

- **事实**：价格、成交量、官方数据
- **解释**：因果链、市场情绪、时间线划分
- 解释性结论必须标注置信度

### 第八步：后续事件关注（强制）

调用 `EarlyWarningEngine().scan(days_ahead=14)` 输出未来两周关键事件，包括：
- 事件名称、日期时间、预期金价波动幅度
- 对当前持仓的影响评估
- 是否需要提前调整仓位（参考 r016/r017）

### 最终建议：条件单审查 + Monitor 事件创建 + 交易建议（强制）

**基于前述八步完整分析**，输出最终建议时，必须包含以下三部分：

#### A. 条件单审查

1. 读取 `data/private/conditional_orders.jsonl`（字段定义见 `data/conditional_orders.example.jsonl`）
2. 筛选 `status = "active"` 的所有条件单
3. **基于完整分析结论**逐条判断：
   - **保留** — 条件单方向与当前分析结论一致，触发价位仍合理
   - **撤销** — 条件单与当前分析结论方向矛盾、触发价位已过时、或基于过时的分析
   - **修改** — 方向正确但触发价位需根据最新分析调整
4. 输出「条件单状态表」：

| 条件单 ID | 类型 | 方向 | 触发价 | 数量 | 状态 | 建议动作 | 原因 |
|-----------|------|------|--------|------|------|----------|------|
| co_xxx | oco | 卖出 | 止盈932/止损852 | 9g | active | **保留** | 方向与观望结论一致，价位合理 |

5. 若分析后需新建条件单，**必须**在 `conditional_orders.jsonl` 中追加记录
6. 若需撤销/修改条件单，**必须**更新对应记录

#### B. Monitor 事件创建

7. 分析结论中的**条件性场景**（如"CPI 低于预期后观察 ETF 回正"、"RSI 跌破 30 后看反弹"），必须创建 monitor 事件：
   - `event_type`: `"monitor"`
   - `status`: `"active"`
   - `trigger_condition`: 清晰的自然语言触发条件
   - `check_frequency`: `"on_analysis"`（每次分析时检查）
   - `action_on_trigger`: 条件满足后的建议动作
   - `expires_at`: 过期时间（可选，如"下次 FOMC 前"）
8. 调用 `calendar.add_event()` 将 monitor 事件写入 `calendar_events.jsonl`
9. **情景预案表中标注 monitor 链接**——分析输出的情景预案表（如 CPI 情景表），凡转成了 monitor 事件的建议动作，必须在对应单元格里标注 `✅ 已加入 Monitor`，确保可溯源：

**CPI 情景预案**:

| CPI 情景 | 金价预期走向 | 建议动作 |
|----------|-------------|---------|
| CPI < 3.8% | 看多，反弹至 920+ | 加仓 5-10g；若接近 OCO 止盈 932.36 则等触发 |
| CPI 3.8-4.2% | 观望，880-910 震荡 | 维持现状，不加不减 |
| CPI > 4.2% | 看空，测试 860 | 860-868 加仓 5-10g → ✅ 已加入 Monitor: `观测: CPI超预期后金价跌至860-868` |

10. 输出「新增 Monitor 事件表」：

| Monitor 名称 | 触发条件 | 检查频率 | 触发后动作 | 过期时间 |
|-------------|----------|----------|-----------|----------|
| 观测: CPI超预期后金价跌至860-868 | XAUUSD ≤ 868 且 CPI > 4.2% | on_analysis | 加仓 5-10g，OCO 止损 852 | 2026-07-30 |
| 观测: ETF资金流回正 | 国际黄金ETF转为日度净流入≥2日 | on_analysis | 确认加仓5-10g | 2026-07-30 |

#### C. 交易建议

10. 明确给出最终建议动作（买/卖/持/加减仓/价位）
11. 标注置信度

> ⚠️ **铁律**：
> - 条件单审查必须在完整分析之后，而非之前。分析前你不知道触发价位是否仍然合理。参见 2026-07-08 事故（卖出 5g 后 890 限价买入单触发买入 11g，导致自我矛盾交易）。
> - 第〇步事件日历同步在分析前就应予完成——事件结果本身不是「观点」，而是需要纳入分析的事实输入。
> - 任何有前置条件的交易建议（"等 X 发生后再做 Y"），必须创建对应的 monitor 事件，否则下次分析会丢失这个跟进条件。

#### D. 推送分析摘要到微信 (强制)

分析报告完成后，必须将**精简摘要**推送到微信，确保用户不在终端前也能收到：

```bash
# 将分析摘要通过 Hermes 推送到微信
bash scripts/push_analysis_to_hermes.sh "🪙 黄金分析 · $(date +%m-%d)

XAUUSD: \$XXXX (±X.X%) | 积存金: ¥XXX
信号: [看多/看空/观望] | 置信度: XX%

关键点:
• ...
• ...

建议: [买/卖/持/加减仓]
下次关注: [事件名] [日期]

(完整报告见终端)"
```

> 🆕 2026-07-18：Hermes 上的轻量哨兵 (`gold_sentinel.py --no-agent`) 只做实时监控推送，**不做完整分析**。完整分析通过本地 Claude Code 运行，结果经 `push_analysis_to_hermes.sh` 推送微信。两者分工：哨兵=实时异动告警，分析=深度决策报告。

---

## 投资军规

完整军规体系（r001-r030）维护在 [`docs/doctrine.md`](docs/doctrine.md)。核心原则包括：

- **仓位管理**：单笔不超过总资产 20%，总敞口不超过 80%，黄金占比超 50% 提示集中风险，不追涨杀跌。
- **情绪纪律**：连续止损后休整、盈利上移止损、多维度确认、必须设止损。
- **操作纪律**：重大数据前提前调整、用条件单代替盯盘、减仓趁反弹。
- **趋势与止盈**：ATR 移动止盈、均线趋势过滤。
- **建仓与估值**：分批建仓、安全边际加仓、黄金仓位再平衡。

涉及交易建议时，军规自查输出 `r001-r030` 判定。

---

## Munger 思维模型（高频使用）

### 投资核心
- **安全边际**: 以显著低于内在价值的价格买入，为判断错误留缓冲
- **市场先生**: 市场报价反映情绪而非价值，利用他而非被他左右
- **坐等投资法**: 买入后长期持有、什么都不做，执行最难
- **能力圈**: 只在真正理解的领域内决策，知道边界比扩大边界重要
- **少下注下大注**: 极端选择性，等待极少高胜率高赔率机会重仓

### 心理防御
- **被剥夺超级反应**: 损失痛苦>>获得快乐，驱动恐慌抛售和底部割肉
- **沉没成本谬误**: 已发生的成本不应影响未来决策
- **社会认同/羊群行为**: 在不确定性中模仿群体，放大泡沫和恐慌
- **避免不一致性倾向**: 大脑抗拒改变已有结论，过滤不利信息
- **锚定偏差**: 过度依赖最先获得的信息（如成本价）
- **奖励和惩罚超级反应**: 激励扭曲认知和行为
- **权威错误影响倾向**: 盲目服从权威（包括"权威机构"研报）
- **过度乐观倾向**: 系统性高估好结果概率、低估坏结果

### 方法论
- **逆向思维**: 先搞清楚什么会导致失败，然后系统性地避免
- **避蠢优于求智**: 减少错误比增加正确更能提升总体结果
- **检查清单方法**: 用系统性方法对抗人类遗漏的必然性
- **二阶效应**: 追问后果的后果，不止看直接影响
- **双轨分析**: 同时从理性（基本面）和心理（人性）两条轨道思考
- **置信度校准**: 主观信心与客观概率吻合，持续预测-反馈迭代
- **地图不是疆域**: 任何模型都是简化，简化意味着遗漏
- **否证思维**: 否证性证据信息价值远大于确认性证据

### 系统思维
- **延迟效应**: 因果之间有时滞（如加息对金价的影响需 12-18 个月显现）
- **自组织临界性**: 系统自发演化到临界状态，小触发可引发大崩盘
- **复杂系统**: 整体行为无法从部件推导，不能用简单直觉处理
- **临界质量与相变**: 迹象线性但变化非线性，最重要变化总出人意料

### 经济学视角
- **机会成本**: 真实代价是放弃的最佳替代方案价值
- **信息不对称**: 卖家比买家更了解商品，永远站在信息优势方
- **激励机制**: 理解激励机制就能以高准确率预测行为
- **代理成本**: 替你做决策的人与你利益不完全一致时你会付出代价

---

## 使用方式

当用户询问金价分析、交易建议、持仓决策时：
	1. 调用 `EarlyWarningEngine().check_recent_results(lookback_days=7)` 查找已发布但未记录结果的事件，搜索权威来源获取实际结果，同步到 `calendar_events.jsonl`（第〇步）。
	2. 读取 `data/private/investor_profile.md` 获取定性画像。
	3. 读取 `data/private/portfolio.yaml` 获取持仓量、成本、止损等数字。
	4. 读取 `docs/doctrine.md` 获取完整军规体系（r001-r030）。
	5. 读取 `data/personal_rules.md` 获取个人敏感参数（具体止损价位、ATR 参数、持仓拆分等）。
	6. 实时获取最新市场数据（含第〇步同步的事件结果）。
	7. 走完第一步到第八步完整分析流程。
	8. 用军规（r001-r030）逐条审查建议动作，必要时引用个人规则中的具体参数。
	9. 用 2-3 个 Munger 模型解释决策逻辑和认知陷阱。
	10. 结合投资者画像给出具体可执行指令（买/卖/持/加减仓/价位）。
	11. **基于完整分析结论**审查活跃条件单，输出「条件单状态表」（保留/撤销/修改 + 原因）。
	12. 标注置信度。

**禁止**：脱离画像约束的建议、没有军规审查的建议、忽略信源质量的分析。

---

## 信息验证协议（Sourcing Truth Verification）

### 强制触发条件
- 任何用于交易决策的数据（价格、CPI、PPI、利率决议、持仓、ETF 流向等）
- 任何带日期/时间戳的事件
- 任何机构观点/目标价/预测

### 来源可信度层级

| 层级 | 来源类型 | 示例 | 可信度 |
|------|----------|------|--------|
| T0 | 一手官方源 | BLS.gov、CME FedWatch、SGE.com.cn、FRED、央行官网 | 最高 |
| T1 | 官方授权数据终端 | Wind、Bloomberg、路透、交易所行情 | 高 |
| T2 | 权威媒体原创 | WSJ、FT、财新、路透、彭博 | 中 |
| T3 | 聚合/自媒体 | 普通财经号、论坛、KOL | 低（需双源交叉） |

### 验证动作
1. **数据类**：至少 1 个 T0 来源确认
2. **事件日期类**：至少 1 个 T0 来源确认，并与用户/已知事实交叉
3. **二手报道类**：追踪到原始出处，否则标注"转述，待核实"
4. **冲突信息**：列出冲突双方，说明采信理由
5. **搜索摘要不可直接作为事实**：搜索引擎结果是发现入口，不是证据

### 输出标注
- `[verified: T0]` — 已用一手来源验证
- `[verified: T2]` — 仅媒体来源，已交叉
- `[unverified]` — 无法验证，仅作参考

### 工具路径
- 优先使用 web-access skill 的 CDP 浏览器访问一手来源官网
- 一手官网不可达时，用 WebSearch 定位官方发布页面，再用 WebFetch/curl 读取
- 严禁仅依赖搜索结果摘要就写入分析或交易建议
